## [2026-08-27 15:58] | Task: 为 CPA WebSocket 增加模型前置门控

### 🤖 Execution Context
* **Agent ID**: `codex`
* **Base Model**: `gpt-5`
* **Runtime**: `Codex Desktop`
* **Git User**: `hubert <hubert@lejian.com>`
* **Branch**: `main`

### 📥 User Query
> CPA WebSocket 请求中，非 `gpt-*` / `codex-*` 模型直接返回 426，不再拨号 CPA；匹配模型仍交给 CPA 按自身设置决定是否接受。

### 🛠 Changes Overview
**Scope:** 网关 WebSocket 路由与测试

**Key Actions:**
- 在 CPA 路由的 WebSocket 目标选择前拦截不匹配模型，让客户端降级 HTTP/SSE。
- 保留 `websocket` 总开关；匹配 `gpt-*` / `codex-*` 的请求继续交给 CPA。
- 增加 `glm` 拒绝与 `gpt`、`codex` 放行断言。

### 🧠 Design Intent (Why)
已知不支持模型无需让 CPA 先握手再返回业务错误；426 必须发生在网关升级前，才能触发 Codex 的 SSE 重试。

### 📊 Change Stats
> 本次局部补丁：2 个代码/测试文件，约 +24 / -3 行；工作树其余改动属于先前任务。

### 📁 Files Modified
- `src/gateway.ts`
- `test/gateway.test.ts`
