## [2026-08-19 19:08] | Task: 手工验收网关改动并新增日志检查工具

### 🤖 Execution Context
* **Agent ID**: `zcode`
* **Base Model**: `builtin:zai-coding-plan/GLM-5.3`
* **Runtime**: `ZCode CLI`
* **Git User**: `hubert`
* **Branch**: `main`

### 📥 User Query
> 现在对完成的进行手工验收和日志检查工具，分析一下按几步执行，需要我手工操作。

### 🛠 Changes Overview
**Scope:** scripts

**Key Actions:**
- **[工具]**: 新增 `scripts/log-check.ts`（bun 直跑、零依赖），汇总按路径分组的请求日志：分组概况、逐条摘要（方法/路径/状态/耗时/model/hint）、realtime 事件、错误摘要。支持 `--tail/--group/--errors/--dir`。
- **[验收]**: 对上一轮改动（routing hint 优先路由、路径分组日志命名、本地时区时间戳）在生产网关上完成合成探针验收，全部通过；工具本身即基于真实日志验证。

### 🧠 Design Intent
上一轮改动上线后缺少快速核查手段：日志按秒分文件、条目多段，直接 `cat` 无法回答「这条请求走了哪个上游、hint 是什么、耗时多少」。工具把 `x-codex-routing-hint` 头与 body 里的 `model` 并列展示，正是验收 hint > payload > fallback 优先级所需的最小信息；错误双写的副本从主列表过滤，避免同屏重复。解析对旧 UTC ISO 与新本地时间戳两种格式兼容。

### 📊 Change Stats
> 数据来自 `git diff --no-index --numstat /dev/null <file>`（新增文件，未提交）。

- **Files changed:** 1
- **Insertions:** +227
- **Deletions:** -0

| File | +Added | -Removed |
| --- | ---: | ---: |
| `scripts/log-check.ts` | +227 | -0 |

### 📁 Files Modified
- `scripts/log-check.ts`
