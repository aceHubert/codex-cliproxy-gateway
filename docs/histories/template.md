## [YYYY-MM-DD HH:mm] | Task: <简短的任务动词>

### 🤖 Execution Context
* **Agent ID**: `如实填写（例如 claude-code / codex / cursor）`
* **Base Model**: `如实填写（例如 claude-opus-4-7 / gpt-5）`
* **Runtime**: `如实填写（例如 Claude Code CLI / VS Code）`
* **Git User**: `git config user.name <email>`
* **Branch**: `当前分支名`

### 📥 User Query
> {用户 Query 原文，或者压缩后的脱敏版本}

### 🛠 Changes Overview
**Scope:** (标注受影响的包，例如 web-desktop/doctor、web-uniapp)

**Key Actions:**
- **[Action 1]**: 简洁说明改动点 (e.g., 实现 xxx 页面)
- **[Action 2]**: (e.g., 优化 xxx 消息转换逻辑)

### 🧠 Design Intent (Why)
*简洁直击要点地说明为什么要这么改。*

### 📊 Change Stats
> 数据来自 `git diff --stat <base>..HEAD`，只统计本次任务相关提交。

- **Files changed:** N
- **Insertions:** +X
- **Deletions:** -Y

| File | +Added | -Removed |
| --- | ---: | ---: |
| `path/to/src/core/file.ts` | +42 | -7 |
| `path/to/src/pages/index.vue` | +15 | -3 |

### 📁 Files Modified
- `path/to/src/core/file.ts`
- `path/to/src/pages/index.vue`
