# 技术债追踪

这里记录那些暂时不阻塞当前任务、但已经值得留档的技术债。

| 日期 | 区域 | 债务描述 | 为什么会存在 | 计划中的后续动作 |
| --- | --- | --- | --- | --- |
| 2026-08-18 | Realtime 上游选择 | 尚未支持通过配置将完整 Live 链路切换至 CLIProxyAPI | 第一阶段先完成本地官方单账号路径，避免提前引入未使用的配置和转发分支 | 按 [CLIProxy Realtime 转发第二阶段实施路径](../cliproxy-realtime-forwarding-plan.md) 实现全局上游选择、认证替换、Location 重写和测试 |
| 2026-08-19 | Realtime provider 门控 | `/v1/realtime/*` 周边路径（client_secrets、sessions、transcription_sessions、translations）不在 `isReservedOfficialRealtimePath` 枚举内，第三方 provider 模式下第三方 Authorization 会落到 ChatGPT backend | 早期按 Codex 实际请求路径逐条枚举；本轮 WebSocket 放行聚焦不扩 scope（已确认） | 改为前缀式门控（`/live` 与 `/realtime` 全子树）并调整 `test/gateway.test.ts` 对应断言 |
| 2026-08-19 | Responses over WebSocket | CLIProxy 侧仅实测过握手 101，帧级协议兼容性未验证；WS 帧内 model 不做前缀改写 | Codex 探测带 routing hint 可定路由，但帧内容改写风险高，先透传观察 | 真机使用中若 cliproxy 会话在 WS 建立后异常断开，收集 `cliproxy-v1-responses-*.log` 的 ws 事件定位；旧的 `websocket: false` 回退已失效，必要时恢复网关侧门控代码或评估帧级 model 改写 |
| 2026-08-20 | Responses over WebSocket 路由 | Codex 预热和无前缀帧可能复用已固定 CPA 的连接；若仅按帧模型名解释为 official，会让 CPA thread 串回官方 | 首次明确的 `cliproxy/` 信号按 `thread-id` 固定 CPA，后续无前缀帧、HTTP/SSE 和 WS 重连沿用 CPA；子智能体独立 WS 通过 `x-codex-parent-thread-id` 单向继承，真机已验证。`session-id` 只用于日志聚合 | 当前版本的独立 `thread_title` 无 parent，按未固定根线程走官方并作为已接受边界另记于 2026-08-30 行；同 session 无父子 thread 的官方隔离由自动化测试覆盖。相关背景见 [20260820-1205 历史记录](../histories/2026-08/20260820-1205-websocket-frame-routing.md) |
| 2026-08-28 | CPA WebSocket 模型门控移除 | 网关不再按 `gpt-/codex-` 前缀预判模型能否走 ws，全部交给 CPA 按请求回退；残余风险是已在 CPA ws 上游跑过 codex 凭据模型的连接里，增量轮（`response.append`/`previous_response_id`）切到非 codex/xai provider 模型会触发 CPA 426 `upstream_http_replay_required` 断连，CLI 可能直接报错而非降级 | 真机验证（[20260828-1133 历史记录](../histories/2026-08/20260828-1133-split-ws-route-reconnect.md)）确认纯非 gpt 会话经 ws 桥接后由 CPA 静默退回 SSE 上游端到端正常，且网关层预判无法防住该断连（只发生在已建立连接内部），保留门控只增加无谓的 426 桥接拒绝 | 使用约束：同一 thread 首次选择 CPA 后保持 CPA，不再切回官方；若真机出现该 426 报错，收集 ws 会话日志确认触发路径，再评估网关侧按帧检测增量轮换模型并提前 1012 重连 |
| 2026-08-30 | CPA WebSocket 网关开关移除 | `websocket` 配置字段与 `models --sync --websocket` 参数删除后，CPA WebSocket 升级一律桥接 CLIProxy，网关失去一键回退 HTTP/SSE 的配置手段；上表 2026-08-19 行的「设 `websocket: false` 回退」动作随之失效 | 上游本就按请求决定 ws/HTTP-SSE 回退（真机已实证 426 降级与重握手两条恢复路径），网关侧开关是多余门控；语义归一到「由上游判断返回」 | 若真机出现 CPA WebSocket 异常且上游无法处理，恢复网关侧门控代码作为回滚（见 [split-websocket 计划决策记录](completed/split-websocket-route-reconnect.md)）；所有配置变更已审计到 `logs/cliproxy-config-*.log`，排查时先核对配置轨迹 |
| 2026-08-30 | 标题线程路由 | `thread_source: thread_title` 的标题线程由桌面端独立进程派生，是无前缀模型（luna）、无 `x-codex-parent-thread-id` 的根线程，三条路由依据（前缀/自身 pin/父线程 pin）全不满足，固定回落官方，且请求体携带主会话首条消息原文一并出网到官方；但它在路由层与主线程零耦合——不共享连接与 pin 状态、不写 `cpaThreads`，真机实测（标题走官方 + 主线程树全 CPA 的同窗口会话）`ws-route-mismatch` 为零，不会引发主线程 CPA 重建。「按最近一次 pin 的时间窗把标题路由到 CPA」方案已评估并推迟：实测 pin 在会话创建（prewarm）时写入、标题在首条消息发出后触发，间隔是用户打字时长（真机 54s）而非毫秒级；标题路由决策比主线程首帧早 0.75s，标题上游拨号随后才完成，决策时刻唯一可用信号是最近 pin 本身，10s 窗口必然落空 | 标题线程无父标识是 Codex 桌面端行为，网关无法改变；luna 单独走官方的用量影响经评估可接受，故保持现状。对 2026-08-20 行「标题在任何前缀信号之前到达」的落地评估结论：时间窗锚定方案可行但窗口需放宽到分钟级，误判面（多开会话时官方会话的标题被锚到别的 CPA 会话）只影响哪个上游付费，不影响内容归属与正确性 | 维持现状；若后续要收敛标题流量，按修正设计实现：`cpaThreads` 改 `Map<threadId, pinnedAt>` 并记录 `lastCpaPinAt`，仅 `thread_source === "thread_title"` 且 `now - lastCpaPinAt < 窗口`（2–5 分钟，建议做成配置字段）时路由 CPA，超窗/解析失败回官方，标题线程自身不 pin；需同步 `schemas/gateway-config.schema.json` 与 `test/gateway.test.ts`（窗口内→CPA、超窗→官方、metadata 缺失/坏 JSON→官方、普通无前缀线程不受影响）。根治需 Codex 侧为标题线程提供父会话路由信号 |
| 2026-08-31 | guardian prewarm 路由竞态 | 带 `x-codex-parent-thread-id` 的无前缀 guardian 可能抢在父线程首次 CPA pin 前选定官方。真机出现 1 次 official `ws-dial-failed`、0 次 official `ws-send`，未通过该 WS 发送推理帧；后续 12 个 guardian 的 19 条 `codex-auto-review` 全部 CPA。若错误官方握手成功，无前缀帧不会触发现有显式 `cliproxy/*` mismatch 防护 | 父线程 pin 与 guardian prewarm 并发；父路由只在目标选择时读取，已在途拨号不会因稍后 pin 重判。独立、无 parent 的 `thread_title` 走官方是另一项既定边界 | 为父线程首次路由决策增加可等待或可重判的 pending 状态，在任何上游拨号前解决带 parent 的 guardian 路由；补充 guardian 先于父 pin 的确定性并发测试、错误官方零拨号/零发送断言，并保持无 parent 的 title thread 默认官方 |
