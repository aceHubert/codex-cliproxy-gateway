# 技术债追踪

这里记录那些暂时不阻塞当前任务、但已经值得留档的技术债。

| 日期 | 区域 | 债务描述 | 为什么会存在 | 计划中的后续动作 |
| --- | --- | --- | --- | --- |
| 2026-09-17（09-18 复核修订） | CodeBuddy/WorkBuddy token 刷新冲突 | 网关只读本机 `.info` 凭据、不做主动刷新。本机 CLI（`@tencent-ai/codebuddy-code` v2.151.0）与桌面扩展共用同一文件且活跃刷新（`lastRefreshTime` 2026-09-17）；CLI `refreshSession` 调 `/v2${prefixPath}/auth/token/refresh`（`prefixPath` 由服务端认证配置下发，插件端为 `/plugin`，包内亦有裸端点调用点），一律带 `X-Refresh-Token` + `X-Auth-Refresh-Source: plugin`——刷新 API 同族，端点变体不构成隔离，第二个刷新方会使先刷新方的 rotation token 静默失效，导致 CLI 登录态或网关凭据无声过期；凭据临近过期（<5 分钟）时网关报 503 让用户回 CLI 重新登录 | 首期聚焦协议转换与消费已登录凭据；accessToken 单次签发约 162 天（2026-09-17 签发、2027-02-26 到期，09-18 复核剩 161 天），只读策略足以长期覆盖；共享 `.info` 上的刷新无法与不配合加锁的 CLI 串行化——codebuddy2api 的正解也是独立凭据副本（导入自己的 auth 目录 + flock 串行 + 原子写回，其文档告诫 "prefer separate browser login or stop using the other client"），需独立设计 | 若长期使用：先落地网关专用登录/独立凭据副本，再实现刷新层（端点与头沿用 CLI 同族 `/v2{prefixPath}/auth/token/refresh` + `X-Refresh-Token` + `X-Auth-Refresh-Source: plugin`，0600 临时文件 + rename 原子写回，进程内刷新串行化）；共享登录场景维持只读，CLI 登录态失效时给出明确诊断而非静默 401；见 [执行计划](completed/codebuddy-proxy-adapter.md) 决策记录 2026-09-18 复核条目 |
| 2026-09-13 | install 回滚与 Web UI 行为的自动化 | install 失败回滚（`launchTouched` + 旧 plist 回写、`reloadLaunchAgent` 强制重载磁盘定义并按 `previousServiceLoaded` 决定拉回/卸载）依赖 launchctl 真实副作用，无可注入 stub，两轮修复均仅以代码审查验收；F8（保存失败后允许重试）属浏览器端行为，`src/ui/` 无测试框架，仅手工验证 | install 流程的 launchctl 调用与 DOM 渲染都不是纯函数；为单个失败分支引入 DI 重构或 UI 测试框架超出 review 修复范围 | 若 install 失败分支出现回归：把 `launchd.ts` 的 launchctl 执行器改为可注入接口并为 install 回滚（含 bootstrap 成功/kickstart 失败的重载路径）补集成用例；若 Web UI 交互复杂度继续上升：为 `src/ui/` 引入轻量组件测试（或 Playwright 冒烟）覆盖 failed→重试路径，见 [执行计划](completed/codex-review-fixes.md) F6/F8 |
| 2026-09-13 | 非 macOS 服务管理（Linux） | 密钥文件后端（credentials.json）已就绪且 `serve` 不设平台门槛，但 install/uninstall/web 仍被 `requireMacOS()` 挡住（launchd/LaunchAgent 无 Linux 等价接入）；「写 key → 网关启动 → 转发带 Authorization」全链路仅经单元测试验收，未在真实 Linux 上跑过 | 服务管理（systemd user unit）与安装器平台分派是独立工作量，本次按用户要求只交付密钥存储兼容、以单元测试验收 | 有 Linux 部署需求时：补 systemd user unit 与 install 的平台分派，并在 Linux 容器/主机跑一次 serve 全链路；见 [执行计划](completed/credentials-json-key-store.md) |
| 2026-09-13 | Windows 适配 | `readSecretFromTerminal` 依赖 `/bin/bash`，win32 上安装交互不可用；`atomicWrite` 的 0600 权限在 NTFS 上是 no-op；credentials.json 文件后端代码平台无关但从未在 Windows 验证 | 用户明确本次不包含 Windows 支持 | 有 Windows 需求时：终端密钥输入改跨平台实现，验证 credentials.json 路径与权限语义，见 [执行计划](completed/credentials-json-key-store.md) |
| 2026-09-13 | Web UI 模型管理 | `/ui` 表单只覆盖 `config` 命令支持的运行期字段；模型选择（`models --sync` 的目录重建、config.toml `model_catalog_json` 受管键、密钥校验）仍只能走 CLI 交互 | models 写路径与 install 耦合（目录重建、受管 TOML 键、chooseModels 终端交互），把它塞进 Web UI 会显著抬高首版风险 | 若要做：先把 `models()` 的非交互路径（`--select` selector 已支持）抽成网关可调用的纯函数，再在 `/ui` 增加目录选择页；见 [Web UI 执行计划](completed/web-config-ui.md) 的范围排除项 |
| 2026-09-13 | z.ai 内置工具形状与共享配额 | 服务端工具全集已实测（`web_search_prime`、`webReader`、`analyze_image`，见 [调研记录](../zai-server-tools-research.md)）：`webReader`（`web_fetch_20250910` 声明后服务端代抓网页）未适配，但 Codex 0.154 无 fetch 内置工具，暂无触发场景；`web_search_prime` 与 `webReader` 共用账号级周/月配额池，耗尽时上游不报错、`tool_result` 内容为 429 错误文本（含重置时间），网关折叠机制把错误当正常搜索结果上抛，每轮白烧 1–3 次搜索往返且 UI 呈现为"搜索成功但结果奇怪"；`analyze_image` 为独立额度不受影响 | z.ai 无公开内置工具文档，共享配额与 429-in-tool_result 形状直到 2026-09-13 探测才发现（当期配额 09-12 已耗尽、09-28 重置）；网关按协议层成功/失败处理，不检查结果内容 | 配额耗尽感知降级（已调研未实施）：响应侧识别 `MCP error -429` + `limit will reset at` 并按重置时间缓存 TTL，有效期内剥离 `web_search_20250305` 声明并在 system 注入降级说明（复用 `image_generation` 剥离模式），到期自动恢复；若 Codex 未来引入 fetch 类内置工具，`webReader` 复用 web_search 折叠机制接入；配额重置后补一次 webReader 成功返回形状实测 |
| 2026-09-13 | ZCode 图片识别指纹依赖 | `analyze_image` 网关代执行的执行信封依赖 z.ai 未公开的 Claude Code 客户端指纹（完整 CC system+tools，`src/zcode/vision-template.json` 逐字提取自 claude-cli 2.1.259 实测请求，112KB）；z.ai 收紧指纹或调整 URL 改写行为会降低执行命中率（现有 3 次重试 + 失败降级兜底，不影响主链路）。另：多图时若模型回传的 imageSource 不含 CDN URL 标识（如只给路径），反查失败会降级为错误说明；续跑腿的上游请求体未单独记入交换日志（仅记腿数） | 服务端代执行实测仅在完整指纹下稳定触发（9/9），最小化信封不触发；z.ai 无公开文档承诺该行为 | 命中率下降时用新版 Claude Code 请求日志重新提取模板资产；若 z.ai 提供公开图片分析端点则替换执行器实现；为续跑腿补充独立的上游请求体日志记录，见 [执行计划](completed/zcode-analyze-image.md) 与 [历史记录](../histories/2026-09/20260913-0140-zcode-analyze-image.md) |
| 2026-09-12 | ZCode 服务端搜索形状依赖 | `web_search` 原生映射按 z.ai 实测私有形状实现（`server_tool_use` 名为 `web_search_prime`、query 走 `input.search_query`、结果为 `tool_result` 文本块、`call_` 前缀 id），并兼容 Anthropic 标准形状；z.ai 未在文档中承诺该行为，上游调整会破坏折叠或历史回放。另：Codex Desktop 是否采信目录 `supports_search_tool` 字段仍未定论（时间线证据不充分），目录开关只作第一道防线，剥离兜底才是主修复 | z.ai Anthropic 兼容端点无公开的服务端工具文档，映射依据为 2026-09-12 三次授权实测（200 探测、流式形状采集、E2E 双搜索折叠） | 上游形状变更时以新实测修正 `src/zcode/request.ts`/`src/zcode/response.ts` 中的形状常量与回放块；在真实 Codex Desktop 会话中确认目录字段表现与交换日志中的剥离注记，见 [历史记录](../histories/2026-09/20260912-1953-zcode-builtin-websearch.md) |
| 2026-09-12 | Codex 目录必填字段 | `catalog.ts` 的 `minimalModelEntry` 与「覆盖规则命中」路径合成的条目不含 `supported_reasoning_levels`；Codex ≥0.154 把该键当必填，任一命中该路径的模型会让整份 `/v1/models` 解码失败、客户端回退内置目录（本次 z.ai 就是该表现，已由厂商预设补齐 2 条，基底未改） | 极简基底字段集按旧版客户端确定；cliproxy 上游条目自带完整字段，长期掩盖了该路径 | 给 `minimalModelEntry` 补 `supported_reasoning_levels`（或在合成后统一校验、缺键补空数组），并补「任何合成条目必含该字段」的断言；改动需复核 newapi 与 `models.json` 覆盖规则两条合成路径，见 [历史记录](../histories/2026-09/20260912-1827-vendor-zai-reasoning-levels.md) |
| 2026-09-12 | ZCode Responses 上游验收 | 新转换链已验证Z.ai Coding Plan / GLM-5.3-Flash单次请求，其余渠道套餐尚未使用真实账户逐项验收；仅支持无状态完整历史，不包含服务端搜索、结构化输出、OAuth刷新或签名流程 | 自动化回归使用虚构凭证和模拟上游；另经用户授权在独立端口仅发送一次真实模型请求并成功，未改动现有运行服务 | 用户授权真实联调后验证套餐行为；以实际上游响应决定是否需要进一步兼容，见 [ZCode Responses计划](completed/zcode-responses.md) |
| 2026-09-11 | ZCode Anthropic 直通 | Coding Plan、体验套餐与 API Key 的生产兼容性尚未实测；未实现 ZCode 条件签名、握手与 PoW | 首版范围仅包含当前 provider 的原样转发及模拟验证，禁止使用真实凭证请求生产模型 API | 用户另行授权后逐模式、逐渠道验证 messages/count_tokens 与流式工具调用；依据实际错误决定是否需要签名，不把 401/403 一律归为签名问题，见 [直通计划](completed/zai-anthropic-passthrough.md) |
| 2026-08-18 | Realtime 上游选择 | 尚未支持通过配置将完整 Live 链路切换至 CLIProxyAPI | 第一阶段先完成本地官方单账号路径，避免提前引入未使用的配置和转发分支 | 按 [CLIProxy Realtime 转发第二阶段实施路径](../cliproxy-realtime-forwarding-plan.md) 实现全局上游选择、认证替换、Location 重写和测试 |
| 2026-08-19 | Realtime provider 门控 | `/v1/realtime/*` 周边路径（client_secrets、sessions、transcription_sessions、translations）不在 `isReservedOfficialRealtimePath` 枚举内，第三方 provider 模式下第三方 Authorization 会落到 ChatGPT backend | 早期按 Codex 实际请求路径逐条枚举；本轮 WebSocket 放行聚焦不扩 scope（已确认） | 改为前缀式门控（`/live` 与 `/realtime` 全子树）并调整 `test/gateway.test.ts` 对应断言 |
| 2026-08-19 | Responses over WebSocket | CLIProxy 侧仅实测过握手 101，帧级协议兼容性未验证；WS 帧内 model 不做前缀改写 | Codex 探测带 routing hint 可定路由，但帧内容改写风险高，先透传观察 | 真机使用中若 cliproxy 会话在 WS 建立后异常断开，收集 `cliproxy-v1-responses-*.log` 的 ws 事件定位；旧的 `websocket: false` 回退已失效，必要时恢复网关侧门控代码或评估帧级 model 改写 |
| 2026-08-20 | Responses over WebSocket 路由 | Codex 预热和无前缀帧可能复用已固定 CPA 的连接；若仅按帧模型名解释为 official，会让 CPA thread 串回官方 | 首次明确的 `cliproxy/` 信号按 `thread-id` 固定 CPA，后续无前缀帧、HTTP/SSE 和 WS 重连沿用 CPA；子智能体独立 WS 通过 `x-codex-parent-thread-id` 单向继承，真机已验证。`session-id` 只用于日志聚合 | 当前版本的独立 `thread_title` 无 parent，按未固定根线程走官方并作为已接受边界另记于 2026-08-30 行；同 session 无父子 thread 的官方隔离由自动化测试覆盖。相关背景见 [20260820-1205 历史记录](../histories/2026-08/20260820-1205-websocket-frame-routing.md) |
| 2026-08-28 | CPA WebSocket 模型门控移除 | 网关不再按 `gpt-/codex-` 前缀预判模型能否走 ws，全部交给 CPA 按请求回退；残余风险是已在 CPA ws 上游跑过 codex 凭据模型的连接里，增量轮（`response.append`/`previous_response_id`）切到非 codex/xai provider 模型会触发 CPA 426 `upstream_http_replay_required` 断连，CLI 可能直接报错而非降级 | 真机验证（[20260828-1133 历史记录](../histories/2026-08/20260828-1133-split-ws-route-reconnect.md)）确认纯非 gpt 会话经 ws 桥接后由 CPA 静默退回 SSE 上游端到端正常，且网关层预判无法防住该断连（只发生在已建立连接内部），保留门控只增加无谓的 426 桥接拒绝 | 使用约束：同一 thread 首次选择 CPA 后保持 CPA，不再切回官方；若真机出现该 426 报错，收集 ws 会话日志确认触发路径，再评估网关侧按帧检测增量轮换模型并提前 1012 重连 |
| 2026-08-30 | CPA WebSocket 网关开关移除 | `websocket` 配置字段与 `models --sync --websocket` 参数删除后，CPA WebSocket 升级一律桥接 CLIProxy，网关失去一键回退 HTTP/SSE 的配置手段；上表 2026-08-19 行的「设 `websocket: false` 回退」动作随之失效 | 上游本就按请求决定 ws/HTTP-SSE 回退（真机已实证 426 降级与重握手两条恢复路径），网关侧开关是多余门控；语义归一到「由上游判断返回」 | 若真机出现 CPA WebSocket 异常且上游无法处理，恢复网关侧门控代码作为回滚（见 [split-websocket 计划决策记录](completed/split-websocket-route-reconnect.md)）；所有配置变更已审计到 `logs/cliproxy-config-*.log`，排查时先核对配置轨迹 |
| 2026-08-30 | 标题线程路由 | `thread_source: thread_title` 的标题线程由桌面端独立进程派生，是无前缀模型（luna）、无 `x-codex-parent-thread-id` 的根线程，三条路由依据（前缀/自身 pin/父线程 pin）全不满足，固定回落官方，且请求体携带主会话首条消息原文一并出网到官方；但它在路由层与主线程零耦合——不共享连接与 pin 状态、不写 `cpaThreads`，真机实测（标题走官方 + 主线程树全 CPA 的同窗口会话）`ws-route-mismatch` 为零，不会引发主线程 CPA 重建。「按最近一次 pin 的时间窗把标题路由到 CPA」方案已评估并推迟：实测 pin 在会话创建（prewarm）时写入、标题在首条消息发出后触发，间隔是用户打字时长（真机 54s）而非毫秒级；标题路由决策比主线程首帧早 0.75s，标题上游拨号随后才完成，决策时刻唯一可用信号是最近 pin 本身，10s 窗口必然落空 | 标题线程无父标识是 Codex 桌面端行为，网关无法改变；luna 单独走官方的用量影响经评估可接受，故保持现状。对 2026-08-20 行「标题在任何前缀信号之前到达」的落地评估结论：时间窗锚定方案可行但窗口需放宽到分钟级，误判面（多开会话时官方会话的标题被锚到别的 CPA 会话）只影响哪个上游付费，不影响内容归属与正确性 | 维持现状；若后续要收敛标题流量，按修正设计实现：`cpaThreads` 改 `Map<threadId, pinnedAt>` 并记录 `lastCpaPinAt`，仅 `thread_source === "thread_title"` 且 `now - lastCpaPinAt < 窗口`（2–5 分钟，建议做成配置字段）时路由 CPA，超窗/解析失败回官方，标题线程自身不 pin；需同步 `schemas/gateway-config.schema.json` 与 `test/gateway.test.ts`（窗口内→CPA、超窗→官方、metadata 缺失/坏 JSON→官方、普通无前缀线程不受影响）。根治需 Codex 侧为标题线程提供父会话路由信号 |
| 2026-08-31 | guardian prewarm 路由竞态 | 带 `x-codex-parent-thread-id` 的无前缀 guardian 可能抢在父线程首次 CPA pin 前选定官方。真机出现 1 次 official `ws-dial-failed`、0 次 official `ws-send`，未通过该 WS 发送推理帧；后续 12 个 guardian 的 19 条 `codex-auto-review` 全部 CPA。若错误官方握手成功，无前缀帧不会触发现有显式 `cliproxy/*` mismatch 防护 | 父线程 pin 与 guardian prewarm 并发；父路由只在目标选择时读取，已在途拨号不会因稍后 pin 重判。独立、无 parent 的 `thread_title` 走官方是另一项既定边界 | 为父线程首次路由决策增加可等待或可重判的 pending 状态，在任何上游拨号前解决带 parent 的 guardian 路由；补充 guardian 先于父 pin 的确定性并发测试、错误官方零拨号/零发送断言，并保持无 parent 的 title thread 默认官方 |
